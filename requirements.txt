# QuantStrata Requirements
# ========================
# Python 3.12+ required (uses dataclass slots=True feature from Python 3.10)

# Core numerical libraries
pandas >= 3.0.0
numpy >= 2.0.0
scipy >= 1.12.0

# Testing
pytest >= 8.0.0

# Visualization
matplotlib >= 3.8.0

# Optional: QuantLib for validation
QuantLib >= 1.40

# Optional: Machine learning
tensorflow >= 2.15.0
